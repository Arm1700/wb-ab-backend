# Dev-friendly Dockerfile for NestJS (watch mode)
FROM node:22-alpine

WORKDIR /usr/src/app

# Install deps first (better layer caching)
COPY package*.json ./
RUN npm install

# Copy the rest of the source
COPY . .

# Prisma client (optional: generated by compose command as well)
# RUN npx prisma generate

EXPOSE 3001
CMD ["npm", "run", "start:dev"]
