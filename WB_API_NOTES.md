# Wildberries API - Примечания о доступности

## ⚠️ Проблемы с доступностью эндпоинтов

### Statistics API - Недоступные эндпоинты

Следующие эндпоинты возвращают **404 "path not found"**:

```
❌ GET /api/v1/supplier/reportDetailByPeriod
❌ GET /api/v1/supplier/stocks
❌ GET /api/v1/supplier/sales
❌ GET /api/v1/supplier/orders
❌ GET /api/v1/supplier/returns
```

### Возможные причины

1. **Токен не имеет нужных прав**
   - Проверьте, что при создании токена выбраны все права: Статистика, Аналитика, Контент

2. **Sandbox режим**
   - Некоторые эндпоинты недоступны в sandbox
   - Нужен production токен от реального магазина

3. **Версия API**
   - Эндпоинты могут иметь другие пути в новой версии API
   - Проверьте актуальную документацию: https://openapi.wildberries.ru/

4. **Тип аккаунта**
   - FBO/FBS аккаунты могут иметь разные эндпоинты
   - Некоторые функции доступны только для определенных типов продавцов

## ✅ Работающие эндпоинты

### Analytics API

```
✅ POST /api/analytics/v3/sales-funnel/products
✅ GET /api/analytics/v1/traffic/summary (возможно)
✅ GET /api/analytics/v1/traffic/daily (возможно)
```

### Content API

```
✅ POST /api/v2/list/goods/filter
✅ GET /content/v2/cards/limits
✅ POST /content/v1/cards/csv/create/task
```

### Advert API

```
✅ GET /adv/v1/promotion/count
✅ POST /adv/v1/export/tasks
```

## 🔧 Как исправить

### Вариант 1: Использовать Analytics API вместо Statistics

Замените Statistics API на Analytics API:

**Было:**
```typescript
GET /api/v1/supplier/sales
```

**Стало:**
```typescript
POST /api/analytics/v3/sales-funnel/products
{
  "dateFrom": "2025-10-01",
  "dateTo": "2025-10-31"
}
```

### Вариант 2: Использовать данные из базы

Текущая реализация уже использует данные из PostgreSQL:
- Трафик → `ProductMetric` (impressions, clicks)
- Воронка продаж → `ProductMetric` (openCount, cartCount, orders, buyoutCount)

### Вариант 3: Получить production токен

1. Зарегистрируйте реальный магазин на WB
2. Создайте токен с полными правами
3. Замените sandbox токен на production

## 📋 Текущая реализация

Все методы экспорта теперь имеют **обработку ошибок**:

```typescript
try {
  const { data } = await this.statisticsClient.get('/api/v1/supplier/stocks', {
    headers: { Authorization: token },
  })
  return data
} catch (error: any) {
  console.error('Error fetching stocks:', error?.response?.data || error.message)
  return [] // Возвращаем пустой массив вместо ошибки
}
```

**Результат:**
- ✅ Приложение не падает с ошибкой
- ✅ Frontend показывает "Нет данных"
- ✅ Ошибки логируются в консоль backend

## 🎯 Рекомендации

### Для разработки (сейчас)

Используйте **тестовые данные из базы**:
- Запустите `POST /api/seed/all` для создания данных
- Все графики и таблицы будут работать с mock данными

### Для production

1. **Получите настоящий WB токен** от реального магазина
2. **Проверьте доступные эндпоинты** через Postman/curl
3. **Обновите пути** в `wb-export.service.ts` согласно документации
4. **Включите нужные права** при создании токена

## 📚 Полезные ссылки

- Официальная документация: https://openapi.wildberries.ru/
- Портал разработчика: https://dev.wildberries.ru/
- Sandbox: https://dev.wildberries.ru/sandbox
- Получение токена: https://seller.wildberries.ru/ → Настройки → API
